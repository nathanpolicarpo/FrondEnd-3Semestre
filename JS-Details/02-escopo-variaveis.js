const nome = `Nathan`;

let sobrenome = `de lima`;

{
    const nome = `Igor`
    let sobrenome = `Policarpo de Lima`;
    console.log( `Nome Completo: ${nome} ${sobrenome}` ); 
}

sobrenome = 700.97
sobrenome = true

console.log( `Nome Completo: ${nome} ${sobrenome}` );

const nomes = ["Nathan", "Igor"];
for (var i = 0; i < nomes.length; i++) {
    console.log(`nome ${i}: ${nomes[i]}`);
}

console.log(i);
